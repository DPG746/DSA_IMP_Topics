// Fixed Slding Window Pattern:

// Build first window
for (int i = 0; i < k; i++) { }

// Sliding
int start = 0;
int end = k;

while (end < n) {
    // remove nums[start]
    // add nums[end]
    start++;
    end++;
}
